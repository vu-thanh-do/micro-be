class NotiConsumer {
    consume(message: string) {
      // Logic xử lý khi nhận message về gửi notification
      console.log("Processing notification message:", message);
      // Thêm logic xử lý gửi email, SMS, v.v...
    }
  }
  
  export default NotiConsumer;
  