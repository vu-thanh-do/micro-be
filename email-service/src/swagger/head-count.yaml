paths:
  /headCount/info/headCountPlan/{id}:
    get:
      tags:
        - Head Count
      summary: Lấy thông tin head count plan
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      security:
        - bearerAuth: []
      responses:
        200:
          description: Success

  /headCount/get-head-count-by-id/{id}:
    get:
      tags:
        - Head Count
      summary: Lấy head count theo ID
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      security:
        - bearerAuth: []
      responses:
        200:
          description: Success